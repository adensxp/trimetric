高量战法每日股票池全量分析（三策略版，每日 16:30 触发）。
V5.3.30 工业级完整版（含建仓时机判定 R090-R094）

## ⚠️ 在主 session 跑（mode=sessionId，session_id=me）

直接输出报告给用户，**不要调 communicate**。

## 🎯 任务目标

每天 16:30 收盘后，对 **494 只 A 股** 全量跑**三策略评分**（短线 + 长线 + 底仓），输出**三套预备池**：
- 短线预备池前 5 名（仓位 7-17%）
- 长线预备池前 3 名（仓位 15-25%）
- 底仓预备池前 2 名（仓位 30-50%）

## 🔁 执行流程

### 1️⃣ 运行三策略全量分析
```bash
cd /workspace/stock_db && python3 daily_pool_analysis_v3.py
```

脚本会：
- 拉取 494 只股票最近 5/20 日数据
- 同时跑三个评分函数（短线 / 长线 / 底仓）
- 集成 V5.3.25 皇冠明珠加权（STRICT 5y ROE ≥ 20% +3 / RELAXED 5y ROE ≥ 15% +1.5）
- 前 N 名写入 daily_prep_pool 表（带 strategy 字段）

### 2️⃣ 输出报告

```
=== 高量战法每日股票池全量分析（三策略版）===
📊 总股票数: 494 只

🏆 短线预备池前 5 名（次日 7-17% 仓位）:
   #1 [code] [name]: 评分 [score] 信号 [n]/6
   ...

🏆 长线预备池前 3 名（次日 15-25% 仓位）:
   ...

🏆 底仓预备池前 2 名（次日 30-50% 仓位）:
   ...

📈 评分分布:
   短线: 高分 ≥7 = [n] / 中分 4-6 = [n] / 低分 <4 = [n]
   长线: 高分 ≥7 = [n] / 中分 4-6 = [n] / 低分 <4 = [n]
   底仓: 高分 ≥7 = [n] / 中分 4-6 = [n] / 低分 <4 = [n]

⏰ 明天 9:00 morning-prep-list 输出三套预备池 + 实时价
```

## 📌 关键约束
- 不要删除自己（长期任务，每工作日 16:30 触发）
- 直接输出报告，不调 communicate
- INSERT OR REPLACE 保证重复跑安全
- 三策略同时跑，互不干扰
- 票单从 `/workspace/stock_db/stocks_batches_500.json` 读取（494 只）
