高量战法盘中完整监控 9:35（持仓 + 预备池建仓机会）。
V5.3.30 工业级完整版（含建仓时机判定 R090-R094）

## ⚠️ 在主 session 跑（mode=sessionId，session_id=me）

直接输出报告给用户，**不要调 communicate**。

## 🎯 9:35 早盘检查

**改进**：每个时间点都跑**完整检查**（建仓 + 加仓 + 减仓 + 清仓），不是按时间点分配功能。

## 🔁 执行步骤

### 1️⃣ 运行完整监控
```bash
cd /workspace/stock_db && python3 monitor.py
```

脚本会检查：
- 持仓股票的清仓/加仓/减仓规则（R005/R024/R005A/R007/R008/R027-R029）
- 预备池前 3 名的建仓机会（高量战法三信号）
- 输出所有触发的告警

### 2️⃣ 输出报告

```
=== 高量战法盘中完整监控（09:35）===

💼 当前持仓: X 只
  [code] [name]: 价 X 浮盈 +X元 (+X%) 量比 X
    [触发规则] 或 ✅ 无触发

📋 预备池建仓检查:
  [strategy] #[rank] [code] [name] 价 X
    [🆕 三信号触发] 或 ⏸️ 三信号未触发

📊 监控总结: X 条触发
```

## 📌 关键约束
- 不要删除自己
- 直接输出报告，不调 communicate
- 完整检查
